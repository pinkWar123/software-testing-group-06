# FR-08 Boundary Value Analysis

Scope: This BVA uses the minimal, meaningful ordered numeric boundaries present in FR-08: cart size (0 vs 1) and the three-way neighborhood around the true cart total (equal / just-below / just-above). The file follows the same table format used in domain testing reports.

Notes:
- `shipping_address` has no format/length constraints in the API spec, so it is not included as a numeric boundary here.
- This BVA only targets functional numeric boundaries; UI/display or security-only observations are recorded separately as gaps or bugs.

| TC | Preconditions | Input | Action | Expected UI Outcome | Expected API Outcome | Actual Result | Status | Equivalence Classes | Severity |
|---|---|---|---|---|---|---|---|---|---|
| TC-FR08-BVA-01 | User logged in (valid JWT). Cart is EMPTY (0 items). | cart size = 0; body `{"total_amount":0, "shipping_address":"123 Le Loi, HCMC"}` | Attempt checkout via UI and `POST /api/checkout` | UI shows empty-cart messaging and should not allow confirmation | API should reject checkout (e.g. 400 Bad Request); no order created; cart remains empty | ![TC-FR08-BVA-01](/HW02_DomainTesting/evidence/screenshots/FR-08_Checkout/TC-FR08-BVA-01.png)UI shows empty cart page (screenshot FR08-03); API returns 200 OK and creates order ID 5 with total_amount 0. | Fail | C2 (cart empty) | High |
| TC-FR08-BVA-02 | User logged in (valid JWT). Cart contains exactly 1 item priced 28000000₫. | cart size = 1; true_total = 28000000; client `total_amount` = 28000000; shipping_address present | Submit checkout via UI and `POST /api/checkout` | UI displays full product list and read-only total 28000000₫; allows confirmation | API creates order with server-computed total 28000000; returns 200 OK; cart cleared after success | ![TC-FR08-BVA-02](/HW02_DomainTesting/evidence/screenshots/FR-08_Checkout/TC-FR08-BVA-02.png) UI shows checkout success (screenshot FR08-01); API creates order ID 3 with total_amount 28000000 but cart remains populated on subsequent `GET /api/cart`. | Fail | C1 (cart ≥1), T1 (equal) | High |
| TC-FR08-BVA-03 | User logged in (valid JWT). Cart contains exactly 1 item priced 28000000₫. | true_total = 28000000; client `total_amount` = 27999999 (just-below); shipping_address present | Submit tampered `POST /api/checkout` (client-sent total lower than true total) | UI has no direct control for tampering; UI should still show correct read-only total before confirmation | API should recompute total from cart and create order with total 28000000. Acceptance of 27999999 indicates domain failure. | API returns 200 OK and creates order ID 6 with total_amount 1000; backend did not recompute total as required. | Fail | C1, T2 (just-below) | Critical |
| TC-FR08-BVA-04 | User logged in (valid JWT). Cart contains exactly 1 item priced 28000000₫. | true_total = 28000000; client `total_amount` = 28000001 (just-above); shipping_address present | Submit tampered `POST /api/checkout` (client-sent total higher than true total) | UI has no direct control for tampering; UI should still show correct read-only total before confirmation | API should recompute total from cart and create order with total 28000000. Acceptance of 28000001 indicates domain failure. | API returns 200 OK and creates order ID 7 with total_amount 28000001; backend accepted tampered total. | Fail | C1, T3 (just-above) | Critical |

Conclusion: No other ordered numeric boundaries are present in FR-08's SRS; adding further numeric BVA cases would require inventing constraints not present in the spec. Record any additional gaps (for example the missing `shipping_address` UI control) in the gap analysis document.
